// Dado um array de nomes, mostre o primeiro e o último.

const nomesArray: string[] = ["Pedro", "Thais", "Joana", "Jorge", "Jonas"];
const ultimoNome: string = nomesArray[nomesArray.length - 1];

alert(`${nomesArray[0]} e ${ultimoNome}`);
