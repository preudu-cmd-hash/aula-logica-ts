// Leia um nome e quantas vezes repetir. Escreva o nome esse tanto de vezes.
let nomeRepetir = prompt("Escreva um nome");
let repetir = Number(prompt("Quantas vezes quer repetí-lo no console?"));

while (repetir > 0) {
  console.log(nomeRepetir);
  repetir--;
}
